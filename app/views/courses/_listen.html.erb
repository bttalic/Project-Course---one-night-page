<%= form_for(current_user.relationships.build(listened_id: @course.id)) do |f| %>
  <div><%= f.hidden_field :listened_id %></div>
  		<input type="text" name="code" id="code" />
  <%= f.submit "Take Course", class: "btn btn-large btn-primary" %>
<% end %>
