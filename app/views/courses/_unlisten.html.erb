<%= form_for(current_user.relationships.find_by_listened_id(@course),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Drop Course", class: "btn btn-large btn-danger" %>
<% end %>