<%= form_for(@notification) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <%= f.hidden_field :course_id %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Post notification" %>
  </div>
  <%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>